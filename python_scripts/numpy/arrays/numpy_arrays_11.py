# Python Numpy empty array
import numpy as np

print("Python Numpy empty array. The best option to be used as a blank canvas.")

#The Python empty function creates an empty array.
#It is not always an empty, and it might load garbage values or zeros, etc. 
#So, don’t be surprised!

print(np.empty(5),'\n')
print(np.empty((4, 4)),'\n')
print(np.empty((2, 3, 4)),'\n')